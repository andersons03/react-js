import './Banner.css';

export const Banner = (params) => {
    return(
        <header className='banner'>
            <img src="./images/banner.png" alt="Primeiro banner" />
        </header>
    );
}